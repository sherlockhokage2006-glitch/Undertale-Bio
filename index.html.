<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Undertale Vibe</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background-color: #0a0a0a;
      color: #ffffff;
      font-family: "Press Start 2P", monospace;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
    }

    h1 {
      font-size: 1rem;
      margin-bottom: 40px;
      opacity: 0.9;
    }

    .character {
      width: 300px; /* Sans maior */
      margin-top: 20px;
      animation: float 3s ease-in-out infinite;
      image-rendering: pixelated;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
  </style>
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
</head>
<body>
  <h1>O que te trouxe ao meu perfil?</h1>
  <img src="https://static.wikia.nocookie.net/undertale/images/6/62/Spr_sans_battle_idle.gif" 
       alt="Sans Undertale" 
       class="character">

  <!-- Música ambiente 8-bit (loop suave) -->
  <audio autoplay loop>
    <source src="https://vgmsite.com/soundtracks/undertale-soundtrack/tujaebjf/022%20-%20sans..mp3" type="audio/mpeg">
    Seu navegador não suporta áudio em HTML5.
  </audio>
</body>
</html>
